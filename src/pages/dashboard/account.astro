---
import Layout from '@layouts/Layout.astro';

import Overview from '@components/dashboard/profile/Overview.svelte';

const meta = {
  nav: 'Dashboard',
  breadcrumbs: [{ label: 'Dashboard', href: '/dashboard' }, { label: 'Account' }],
  header: 'Account',
};
---

<Layout {...meta}>
  <Overview client:only="svelte" />
</Layout>
