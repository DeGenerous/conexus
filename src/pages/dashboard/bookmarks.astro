---
import Layout from '@layouts/Layout.astro';

import BookmarksPage from '@components/dashboard/profile/Bookmarks.svelte';

const meta = {
  nav: 'Dashboard',
  breadcrumbs: [{ label: 'Dashboard', href: '/dashboard' }, { label: 'Bookmarks' }],
  header: 'Bookmarks',
};
---

<Layout {...meta}>
  <BookmarksPage client:only="svelte" />
</Layout>
