---
import Layout from '@layouts/Layout.astro';

import ExploreStory from '@components/dashboard/dream/manage/ExploreStory.svelte';

const { topic } = Astro.params;

const meta = {
  nav: topic,
};

if (!topic) {
  Astro.redirect('/');
}
---

<main>
  <Layout {...meta}>
    <ExploreStory topic_name={topic} client:load />
  </Layout>
</main>
