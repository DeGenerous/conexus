---
import Layout from '@layouts/Layout.astro';

import ManageStories from '@components/dashboard/dream/ManageStories.svelte';

const { topic } = Astro.params;

if (!topic) {
  Astro.redirect('/');
}

const meta = {
  header: 'Manage Stories',
  nav: 'Manage Stories',
  arrow: '/dashboard/dream',
  subheading:
    'Edit, organize, and enhance your creations—fine-tune every detail to perfection.',
};
---

<Layout {...meta}>
  <main>
    <ManageStories client:load />
  </main>
</Layout>
