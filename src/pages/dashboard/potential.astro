---
import Layout from '@layouts/Layout.astro';

import Portrait from '@components/dashboard/omnihub/Portrait.svelte';

import { NAV_ROUTES } from '@constants/routes';

const meta = {
  header: 'Portrait',
  nav: 'Portrait',
  breadcrumbs: [
    { label: 'Dashboard', href: NAV_ROUTES.DASHBOARD },
    { label: 'OmniHub', href: NAV_ROUTES.OMNIHUB },
    { label: 'Portrait' },
  ],
};
---

<Layout {...meta}>
  <Portrait client:idle />
</Layout>
