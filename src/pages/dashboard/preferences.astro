---
import Layout from '@layouts/Layout.astro';

import PreferencesPage from '@components/dashboard/profile/Preferences.svelte';

const meta = {
  nav: 'Dashboard',
  breadcrumbs: [
    { label: 'Dashboard', href: '/dashboard' },
    { label: 'Preferences' },
  ],
  header: 'Preferences',
};
---

<Layout {...meta}>
  <PreferencesPage client:only="svelte" />
</Layout>
