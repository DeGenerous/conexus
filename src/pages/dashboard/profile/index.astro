---
import Layout from '@layouts/Layout.astro';

import Profile from '@components/dashboard/Profile.svelte';

const arrow = true;

const meta = {
  header: 'Profile',
};
---

<Layout {...meta} {arrow}>
  <Profile client:load />
</Layout>
