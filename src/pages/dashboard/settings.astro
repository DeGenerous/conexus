---
import Layout from '@layouts/Layout.astro';

import SettingsPage from '@components/dashboard/profile/Settings.svelte';

const meta = {
  nav: 'Dashboard',
  arrow: '/dashboard',
  header: 'Settings',
};
---

<Layout {...meta}>
  <SettingsPage client:only="svelte" />
</Layout>
