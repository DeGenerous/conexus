---
import Layout from '@layouts/Layout.astro';

import Dream from '@components/Dream.svelte';

const meta = {
  nav: 'Dream',
  breadcrumbs: [{ label: 'Dashboard', href: '/dashboard' }, { label: 'Dream' }],
  header: 'Dream',
};
---

<Layout {...meta}>
  <Dream client:only="svelte" />
</Layout>
