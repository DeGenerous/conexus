---
import Layout from '@layouts/Layout.astro';
import UseReferral from '@components/routes/Referral.svelte';

const meta = {
  title: 'Referral',
  nav: 'Referral',
};
---

<Layout {...meta}>
  <UseReferral client:load />
</Layout>
