---
import Layout from '@layouts/Layout.astro';
import UseReferral from '@components/routes/Referral.svelte';

const meta = {
  nav: 'Referral',
  header: 'Referral Code',
};
---

<Layout {...meta}>
  <UseReferral client:idle />
</Layout>
