---
import Layout from '@layouts/Layout.astro';
import ResetPassword from '@components/routes/ResetPassword.svelte';

const { token } = Astro.params;

const meta = {
  nav: 'Reset Password',
  header: 'Reset Password',
};
---

<Layout {...meta}>
  <ResetPassword {token} client:load />
</Layout>
