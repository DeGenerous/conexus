---
import Layout from '@layouts/Layout.astro';
import VerifyEmail from '@components/routes/VerifyEmail.svelte';

const { token } = Astro.params;

const arrow = true;
---

<Layout {arrow}>
  <VerifyEmail token={token} client:load />
</Layout>
