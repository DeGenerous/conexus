---
import Layout from '@layouts/Layout.astro';
import VerifyEmail from '@components/routes/VerifyEmail.svelte';

const { token } = Astro.params;

const meta = {
  nav: 'Verify Email',
  header: 'Email Verification',
  arrow: '/',
};
---

<Layout {...meta}>
  <VerifyEmail token={token} client:idle />
</Layout>
